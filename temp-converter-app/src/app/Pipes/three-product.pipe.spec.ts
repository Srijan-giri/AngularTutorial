import { ThreeProductPipe } from './three-product.pipe';

describe('ThreeProductPipe', () => {
  it('create an instance', () => {
    const pipe = new ThreeProductPipe();
    expect(pipe).toBeTruthy();
  });
});
